<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.muhammedbas.tarifdefterim">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNO">
        <activity android:name=".LoginRegister.LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginRegister.RegisterScreen" />
        <activity
            android:name=".HomeScreen"
            android:theme="@style/AppTheme" />
        <activity android:name=".SettingPage.SettingPage" />
        <activity android:name=".SearchScreen" />
        <activity android:name=".AddRecipes"
            android:theme="@style/AppTheme" />
        <activity android:name=".RecipeScreen"
            android:theme="@style/AppTheme" />
        <activity android:name=".RecipeFullScreen"
            android:theme="@style/AppTheme" />
        <activity android:name=".SettingPage.HelpScreen"
            android:theme="@style/AppTheme"/>
        <activity android:name=".SettingPage.ContactScreen"
            android:theme="@style/AppTheme"/>
        <activity android:name=".SettingPage.FavoriteScreen"
            android:theme="@style/AppTheme"/>
    </application>

</manifest>